__pycache__/
*.pyc
.env
.venv/
venv/
.pytest_cache/
.DS_Store
# NOTE: data/ is intentionally NOT ignored — committed outputs let a reviewer
# read memos without re-running the pipeline (an assessment requirement).
